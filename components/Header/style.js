import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#637ae0'
    },
    innerContainer: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        margin: 10
    },
    text: {
        fontSize: 24,
        fontWeight: 'bold',
        color: 'white'
    },
    logOutText: {
        fontSize: 16
    }
})

export default styles;
